/**
 * Single source of truth for the user-visible app version.
 * Bump this in the same commit as the version's git stamp; the value
 * is rendered in the Footer so deployments are easy to identify.
 *
 * Keep in sync with package.json's `version` field.
 */
export const APP_VERSION = "0.3.0"
